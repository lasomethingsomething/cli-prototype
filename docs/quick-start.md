### Quick Start

The full guided journey:

```bash
model-cli wizard
```

Follow the interactive prompts.

Individual steps for automation:

```bash
model-cli package
model-cli sign
model-cli verify
model-cli deploy
```
